pipeline {
    agent any  // Or you can use 'agent none' or 'agent { label "your_label" }'

    stages {
        stage('Example') {
            steps {
                script {
                    // Your Docker-related steps here
                    docker.image('liquibase/liquibase').run {
                        // Your steps inside the Docker container
                        sh 'echo "Hello from Docker"'
                    }
                }
            }
        }
    }
}
